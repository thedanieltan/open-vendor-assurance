# v0.1.0 Public Launch Readiness Record

This record captures the final pre-launch readiness state for OpenVA v0.1.0.

It does not tag a release, publish a release, or change repository visibility. Those remain manual maintainer decisions.

## Current decision state

```text
status: release-candidate-ready
release: v0.1.0
profileId: openva.public-metadata.v1
schemaVersion: openva-export-pack.v1
packId: open-vendor-assurance
```

## Completed dry-run checks

The following checks completed successfully on `main` before this readiness record:

```bash
python -m tools.openva.validate validate
pytest -q
python -m tools.openva.release_smoke
```

Observed local result:

```text
OpenVA validation passed.
123 passed.
Release smoke test passed.
```

Repository hygiene check after P42:

```bash
git ls-files | grep -E '(__pycache__|\.pyc$|\.pyo$)'
```

Observed local result:

```text
no tracked Python bytecode files
```

## Observation queue result

The release-candidate dry run produced a source-refinement queue summary:

```text
Total observed sources: 58
Human review required: 36
bot_protected: 26
fetch_failed: 9
size_limited: 1
```

This is treated as a non-blocking source-quality backlog.

It does not block v0.1.0 because the observed queue summary did not identify release-blocking categories such as unsafe URLs, policy-boundary violations, private material exposure, advisory language, generated-file drift, schema/pack incompatibility, release smoke failure, conformance fixture failure, or broken workflow behavior.

## Public launch blockers

Block public launch if any of these appear before cutover:

- failing `validate / validate` on `main`;
- failing release-candidate workflow;
- generated `openva-pack.json` or `indexes/` drift;
- pack/schema/profile incompatibility;
- tracked bytecode/cache artifacts;
- raw vendor document mirrors committed by default;
- private, gated, customer-specific, portal, NDA, SOC report, ISO certificate, or extracted full-text material;
- advisory wording or vendor approval/scoring claims;
- workflow permission escalation;
- issue templates, CODEOWNERS, SECURITY.md, or governance docs missing from the public launch path.

## Non-blocking launch backlog

Do not block v0.1.0 on these items alone:

- bot-protected public vendor pages;
- transient fetch failures;
- size-limited public pages;
- better source candidates not yet found;
- native-language summary improvements;
- catalog expansion backlog;
- source-refinement queue items that remain metadata-only and public-source-only.

## Required manual checks before visibility change

Before changing repository visibility, maintainers should confirm:

```text
main branch protection is configured
validate / validate is required on main
CODEOWNERS review expectations are configured where supported
SECURITY.md private reporting guidance is visible
issue templates render correctly
blank issues are disabled if desired
README, DISCLAIMER, CONTRIBUTING, GOVERNANCE, and docs/index.md are current
```

## Manual cutover sequence

Recommended sequence:

1. Run the manual `release-candidate` workflow on `main`.
2. Download and inspect `release-artifacts.json`.
3. Run or review the latest `observe-report` output.
4. Confirm source-refinement queue contains no release blockers.
5. Confirm `validate / validate` is green on `main`.
6. Decide whether to tag `v0.1.0` before or after public visibility.
7. Change repository visibility only through GitHub repository settings.
8. Publish a launch note that preserves the public-source-only, metadata-first, non-advisory boundary.

## Launch positioning

Use:

```text
OpenVA is a public-source-only, metadata-first registry of vendor-published assurance references.
```

Do not use:

```text
compliance database
legal document archive
vendor risk scoring system
certification authority
procurement recommendation engine
```

## Final maintainer decision

The final release/tag/visibility decision is manual.

This document records that the substrate is ready for that decision; it does not perform the decision.
