# Human download template follow-up

This branch retires the local compiler's old branded/over-specified CSV contract and replaces it with the simple compiled vendor information template.

The static browser matcher still needs the same shape applied in `site/src/app.js`, but that path is outside the current `WP-OPENVA-HINT-ONLY-COMPILER-CLI-01` scope. Apply it in a follow-up browser-output PR under an appropriate result-pack/static-site scope.
