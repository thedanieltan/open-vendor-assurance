name: owner payment vendor materializer 2

on:
  push:
    branches:
      - catalog/owner-payments-expansion-2
    paths:
      - catalog-batches/owner-payments-expansion-2.yaml

permissions:
  contents: write

concurrency:
  group: owner-payments-expansion-2-materializer
  cancel-in-progress: false

jobs:
  materialize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: catalog/owner-payments-expansion-2
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Install package
        run: pip install -e '.[dev]'
      - name: Generate, observe, and validate payment tranche
        id: tranche
        shell: bash
        run: |
          set +e
          bash -euo pipefail <<'RUN' > /tmp/materializer.log 2>&1
          python -m tools.openva.catalog_batch catalog-batches/owner-payments-expansion-2.yaml --build-indexes

          find \
            data/vendors/worldpay/sources \
            data/vendors/fiserv/sources \
            data/vendors/fis/sources \
            data/vendors/global-payments/sources \
            data/vendors/razorpay/sources \
            -type f -name '*.yaml' | sort > /tmp/owner-payment-2-source-paths.txt

          python -m tools.openva.source_verification verify \
            --source-path-file /tmp/owner-payment-2-source-paths.txt \
            --output /tmp/owner-payment-2-verification.json

          baseline_args=()
          if [ -f maintenance/source-observations/latest-observations.json ]; then
            baseline_args=(--baseline maintenance/source-observations/latest-observations.json)
          fi

          python -m tools.openva.observation_ledger build \
            --verification-report /tmp/owner-payment-2-verification.json \
            --output-dir /tmp/owner-payment-2-observations \
            --run-id owner-payments-expansion-2 \
            "${baseline_args[@]}"

          python -m tools.openva.observation_ledger install-latest \
            --latest /tmp/owner-payment-2-observations/latest-observations.json

          python -m tools.openva.validate validate
          python -m pytest -q \
            tests/test_catalog_guard.py \
            tests/test_source_type_labels.py \
            tests/test_generated_exports.py \
            tests/test_source_observation_ledger.py
          RUN
          status=$?
          set -e

          if [ "$status" -ne 0 ]; then
            mkdir -p maintenance/generated
            {
              echo '# Owner payment tranche 2 diagnostic'
              echo
              echo 'The fail-fast generator, observation, or validation lane exited non-zero.'
              echo
              echo '```text'
              tail -n 300 /tmp/materializer.log
              echo '```'
            } > maintenance/generated/owner-payments-expansion-2-diagnostic.md
            echo 'success=false' >> "$GITHUB_OUTPUT"
          else
            rm -f maintenance/generated/owner-payments-expansion-2-diagnostic.md
            echo 'success=true' >> "$GITHUB_OUTPUT"
          fi
      - name: Remove one-time inputs after successful validation
        if: steps.tranche.outputs.success == 'true'
        run: |
          rm catalog-batches/owner-payments-expansion-2.yaml
          rm .github/workflows/owner-global-vendors-2-materialize.yml
      - name: Commit materialized result
        shell: bash
        run: |
          git diff --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          if git diff --cached --quiet; then
            echo 'No materializer output was produced.'
            exit 1
          fi
          if [ "${{ steps.tranche.outputs.success }}" = 'true' ]; then
            git commit -m 'Catalog: add five payment vendors'
          else
            git commit -m 'Catalog: capture second payment materializer failure'
          fi
          git push origin HEAD:catalog/owner-payments-expansion-2
      - name: Fail after publishing diagnostic
        if: steps.tranche.outputs.success != 'true'
        run: exit 1
